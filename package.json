{
	"name": "tool-qr-generator",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc -b && vite build",
		"typecheck": "tsc -b",
		"lint": "biome check .",
		"test": "bun run lint && bun run build",
		"format": "biome format . --write",
		"preview": "vite preview"
	},
	"dependencies": {
		"@rebuildup/my-web-tools-ui": "link:../../ui/src",
		"react": "^19.2.8",
		"react-dom": "^19.2.8"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@vitejs/plugin-react": "^4.3.4",
		"typescript": "~5.6.2",
		"vite": "^6.0.5"
	}
}
